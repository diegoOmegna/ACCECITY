<h1> index </h1>

<div class="container">
<ul>
  <% @places.each do |place| %>
    <li><%= place.name %></li>
    <li><%= place.details %></li>
    <li><%= place.address% ></li>
  <% end %>
</ul>
</div>

<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>
